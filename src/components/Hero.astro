<section class="hero" aria-labelledby="hero-heading">
  <div class="hero-bg" aria-hidden="true">
    <div class="hero-photo"></div>
    <div class="hero-overlay"></div>
  </div>

  <div class="hero-content container">
    <p class="hero-label hero-anim" style="--delay: 0.2s">Kebab & Grillades · Lyon 3</p>
    <h1 id="hero-heading" class="hero-title hero-anim" style="--delay: 0.4s">
      <span class="hero-title-line">Restaurant</span>
      <span class="hero-title-accent">Mosaique</span>
    </h1>
    <p class="hero-subtitle hero-anim" style="--delay: 0.6s">
      Viande grillée à la broche, frites fraîches maison,<br />
      pain cuit sur place. Le vrai goût depuis Lyon 3.
    </p>
    <div class="hero-actions hero-anim" style="--delay: 0.8s">
      <a href="#carte" class="btn btn-primary">Voir la carte</a>
      <a href="tel:+33478177487" class="btn btn-outline">Appeler</a>
    </div>

    <div class="hero-badges hero-anim" style="--delay: 1s">
      <span class="badge">Halal</span>
      <span class="badge">Sur place & à emporter</span>
      <span class="badge">Livraison</span>
    </div>
  </div>

  <div class="hero-scroll" aria-hidden="true">
    <svg width="20" height="32" viewBox="0 0 20 32" fill="none">
      <rect x="1" y="1" width="18" height="30" rx="9" stroke="currentColor" stroke-width="1.5"/>
      <circle class="hero-scroll-dot" cx="10" cy="10" r="2.5" fill="currentColor"/>
    </svg>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background: var(--color-charcoal);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
  }

  .hero-photo {
    position: absolute;
    inset: 0;
    background: url('/images/hero.jpg') center / cover no-repeat;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: rgba(26, 23, 20, 0.65);
  }

  .hero-content {
    position: relative;
    text-align: center;
    padding-block: 8rem 4rem;
    z-index: 2;
  }

  .hero-anim {
    opacity: 0;
    transform: translateY(20px);
    animation: hero-enter 0.8s var(--ease-out-expo) forwards;
    animation-delay: var(--delay, 0s);
  }

  @keyframes hero-enter {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-anim {
      opacity: 1;
      transform: none;
      animation: none;
    }
  }

  .hero-label {
    font-size: var(--text-sm);
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-amber);
    background: rgba(232, 148, 58, 0.1);
    border: 1px solid rgba(232, 148, 58, 0.2);
    display: inline-block;
    padding: 0.35rem 1.25rem;
    border-radius: 100px;
    margin-bottom: var(--space-md);
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: var(--text-hero);
    font-weight: 700;
    line-height: 0.95;
    color: var(--color-white);
    margin-bottom: var(--space-md);
  }

  .hero-title-line {
    display: block;
    font-size: 0.5em;
    letter-spacing: 0.08em;
    color: var(--color-text-muted);
    font-family: var(--font-body);
    font-weight: 500;
    text-transform: uppercase;
    margin-bottom: 0.2em;
  }

  .hero-title-accent {
    display: block;
    font-style: italic;
    background: linear-gradient(135deg, var(--color-amber), var(--color-red-light));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: 1.7;
    max-width: 32rem;
    margin-inline: auto;
  }

  .hero-actions {
    display: flex;
    gap: var(--space-sm);
    justify-content: center;
    flex-wrap: wrap;
    margin-top: var(--space-lg);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 500;
    padding: 0.85rem 2rem;
    border-radius: 100px;
    transition: all var(--duration-normal) var(--ease-out-expo);
    cursor: pointer;
    border: none;
  }

  .btn-primary {
    background: var(--color-amber);
    color: var(--color-charcoal);
  }

  .btn-primary:hover {
    background: var(--color-amber-light);
    transform: translateY(-1px);
    box-shadow: 0 4px 24px rgba(232, 148, 58, 0.3);
  }

  .btn-outline {
    background: transparent;
    color: var(--color-cream);
    border: 1.5px solid rgba(245, 240, 232, 0.3);
  }

  .btn-outline:hover {
    background: var(--color-cream);
    color: var(--color-charcoal);
    transform: translateY(-1px);
  }

  .hero-badges {
    display: flex;
    gap: var(--space-xs);
    justify-content: center;
    flex-wrap: wrap;
    margin-top: var(--space-lg);
  }

  .badge {
    font-size: var(--text-xs);
    font-weight: 500;
    color: var(--color-text-muted);
    padding: 0.3rem 0.75rem;
    border: 1px solid rgba(245, 240, 232, 0.12);
    border-radius: 100px;
  }

  .hero-scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    color: var(--color-amber);
    opacity: 0.4;
  }

  .hero-scroll-dot {
    animation: scroll-hint 2s ease-in-out infinite;
  }

  @keyframes scroll-hint {
    0%, 100% { transform: translateY(0); opacity: 1; }
    50% { transform: translateY(8px); opacity: 0.3; }
  }
</style>
