<section class="hero" aria-labelledby="hero-heading">
  <div class="hero-bg" aria-hidden="true">
    <div class="hero-photo"></div>
    <div class="hero-overlay"></div>
    <div class="hero-vignette"></div>
  </div>

  <div class="hero-content container">
    <!-- Magazine meta-line -->
    <div class="hero-meta hero-anim" style="--delay: 0.1s">
      <span class="meta-num">N°&nbsp;01</span>
      <span class="meta-sep"></span>
      <span>Lyon 3<sup>e</sup></span>
      <span class="meta-sep"></span>
      <span>Kebab & Grillades</span>
      <span class="meta-sep"></span>
      <span>Halal · Turc</span>
    </div>

    <h1 id="hero-heading" class="hero-title hero-anim" style="--delay: 0.3s">
      <span class="hero-title-line">Restaurant</span>
      <span class="hero-title-accent">Mosaique</span>
    </h1>

    <!-- Editorial lede, two-column layout -->
    <div class="hero-lede hero-anim" style="--delay: 0.55s">
      <p class="hero-kicker">
        <span class="kicker-rule"></span>
        Édition 2026 — Avenue Félix Faure
      </p>
      <p class="hero-subtitle">
        Kebab à la broche, grillades et spécialités turques.
        <em>Simple, généreux,</em> au cœur de Lyon 3<sup>e</sup>.
      </p>
    </div>

    <div class="hero-actions hero-anim" style="--delay: 0.75s">
      <a href="#carte" class="btn btn-primary">
        Voir la carte
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <line x1="5" y1="12" x2="19" y2="12"/>
          <polyline points="12 5 19 12 12 19"/>
        </svg>
      </a>
      <a href="tel:+33478177487" class="btn btn-ghost">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
        </svg>
        04 78 17 74 87
      </a>
    </div>

    <!-- Footer strip: delivery + rating -->
    <div class="hero-strip hero-anim" style="--delay: 0.95s">
      <div class="strip-col">
        <span class="strip-label">Google</span>
        <div class="strip-rating">
          <span class="strip-score">4,3</span>
          <span class="strip-stars" aria-hidden="true">
            <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 4.84 5.34.78-3.87 3.77.91 5.33L10 13.27l-4.77 2.51.91-5.33L2.27 6.68l5.34-.78z"/></svg>
            <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 4.84 5.34.78-3.87 3.77.91 5.33L10 13.27l-4.77 2.51.91-5.33L2.27 6.68l5.34-.78z"/></svg>
            <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 4.84 5.34.78-3.87 3.77.91 5.33L10 13.27l-4.77 2.51.91-5.33L2.27 6.68l5.34-.78z"/></svg>
            <svg viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 4.84 5.34.78-3.87 3.77.91 5.33L10 13.27l-4.77 2.51.91-5.33L2.27 6.68l5.34-.78z"/></svg>
          </span>
          <span class="strip-count">3 600+ avis</span>
        </div>
      </div>
      <div class="strip-divider" aria-hidden="true"></div>
      <div class="strip-col">
        <span class="strip-label">Horaires</span>
        <span class="strip-hours">Lun–Sam · 11h — 23h</span>
      </div>
    </div>
  </div>

  <div class="hero-scroll" aria-hidden="true">
    <span class="scroll-label">Scroll</span>
    <span class="scroll-line"></span>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    overflow: hidden;
    background: var(--color-charcoal);
    isolation: isolate;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
  }

  .hero-photo {
    position: absolute;
    inset: 0;
    background: url('/images/hero.jpg') center / cover no-repeat;
    transform: scale(1.05);
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(115deg, rgba(26, 23, 20, 0.85) 0%, rgba(26, 23, 20, 0.55) 55%, rgba(26, 23, 20, 0.9) 100%),
      linear-gradient(to bottom, transparent 40%, rgba(26, 23, 20, 0.95) 100%);
  }

  .hero-vignette {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 30% 40%, transparent 0%, rgba(26, 23, 20, 0.5) 80%);
  }

  .hero-content {
    position: relative;
    z-index: 2;
    padding-block: 9rem 4rem;
    max-width: 100%;
    display: grid;
    gap: var(--space-md);
    align-items: start;
  }

  .hero-anim {
    opacity: 0;
    transform: translateY(24px);
    animation: hero-enter 1s var(--ease-out-expo) forwards;
    animation-delay: var(--delay, 0s);
  }

  @keyframes hero-enter {
    to { opacity: 1; transform: translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-anim {
      opacity: 1;
      transform: none;
      animation: none;
    }
    .hero-photo { transform: none; }
  }

  /* ─── Meta line ─── */
  .hero-meta {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .meta-num {
    color: var(--color-amber);
    font-weight: 600;
  }

  .meta-sep {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: currentColor;
    opacity: 0.4;
  }

  /* ─── Title ─── */
  .hero-title {
    font-family: var(--font-display);
    font-size: var(--text-hero);
    font-weight: 700;
    line-height: 0.88;
    color: var(--color-cream);
    letter-spacing: -0.02em;
    max-width: 14ch;
  }

  .hero-title-line {
    display: block;
    font-size: 0.28em;
    letter-spacing: 0.22em;
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    font-weight: 500;
    text-transform: uppercase;
    margin-bottom: 0.8em;
  }

  .hero-title-line::before {
    content: '';
    display: inline-block;
    width: 2.5rem;
    height: 1px;
    background: var(--color-amber);
    vertical-align: middle;
    margin-right: 0.8rem;
    opacity: 0.8;
  }

  .hero-title-accent {
    display: block;
    font-style: italic;
    background: linear-gradient(135deg, var(--color-amber) 0%, var(--color-amber-light) 50%, var(--color-red-light) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* ─── Lede ─── */
  .hero-lede {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-md) var(--space-lg);
    align-items: start;
    max-width: 46rem;
    margin-top: var(--space-xs);
  }

  .hero-kicker {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-amber);
    display: flex;
    align-items: center;
    gap: 0.6rem;
    white-space: nowrap;
    padding-top: 0.4rem;
  }

  .kicker-rule {
    width: 1.75rem;
    height: 1px;
    background: currentColor;
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--color-cream);
    line-height: 1.55;
    font-weight: 400;
  }

  .hero-subtitle em {
    font-family: var(--font-display);
    font-style: italic;
    color: var(--color-amber);
    font-weight: 700;
  }

  /* ─── Actions ─── */
  .hero-actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-top: var(--space-sm);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 500;
    padding: 0.95rem 1.75rem;
    border-radius: 2px;
    transition: all var(--duration-normal) var(--ease-out-expo);
    cursor: pointer;
    border: none;
    letter-spacing: 0.02em;
  }

  .btn-primary {
    background: var(--color-amber);
    color: var(--color-charcoal);
    border: 1px solid var(--color-amber);
  }

  .btn-primary:hover {
    background: var(--color-amber-light);
    border-color: var(--color-amber-light);
    transform: translateY(-1px);
    box-shadow: 0 12px 32px -8px rgba(232, 148, 58, 0.5);
  }

  .btn-primary svg {
    transition: transform var(--duration-normal) var(--ease-out-expo);
  }

  .btn-primary:hover svg {
    transform: translateX(4px);
  }

  .btn-ghost {
    background: transparent;
    color: var(--color-cream);
    border: 1px solid rgba(245, 240, 232, 0.22);
  }

  .btn-ghost:hover {
    background: rgba(245, 240, 232, 0.08);
    border-color: rgba(245, 240, 232, 0.4);
    transform: translateY(-1px);
  }

  /* ─── Info strip ─── */
  .hero-strip {
    margin-top: var(--space-lg);
    display: grid;
    grid-template-columns: auto 1px auto 1px auto;
    gap: var(--space-md);
    align-items: center;
    padding: var(--space-md) 0;
    border-top: 1px solid rgba(245, 240, 232, 0.12);
    border-bottom: 1px solid rgba(245, 240, 232, 0.12);
    max-width: 48rem;
  }

  .strip-col {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .strip-label {
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  .strip-divider {
    width: 1px;
    height: 2.4rem;
    background: rgba(245, 240, 232, 0.12);
  }

  .strip-links, .strip-rating {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: var(--text-sm);
    color: var(--color-cream);
  }

  .strip-link {
    transition: color var(--duration-fast) ease;
  }

  .strip-link:hover { color: var(--color-amber); }

  .strip-sep {
    color: var(--color-text-muted);
  }

  .strip-score {
    font-family: var(--font-display);
    font-weight: 700;
    font-style: italic;
    font-size: 1.2rem;
    color: var(--color-amber);
  }

  .strip-stars {
    display: inline-flex;
    color: var(--color-amber);
  }

  .strip-stars svg {
    width: 12px;
    height: 12px;
  }

  .strip-count {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
  }

  .strip-hours {
    font-size: var(--text-sm);
    color: var(--color-cream);
  }

  @media (max-width: 720px) {
    .hero-content { padding-block: 7rem 3rem; }
    .hero-lede { grid-template-columns: 1fr; gap: var(--space-sm); }
    .hero-kicker { padding-top: 0; }
    .hero-strip {
      grid-template-columns: 1fr;
      gap: var(--space-sm);
    }
    .strip-divider {
      width: 100%;
      height: 1px;
    }
  }

  /* ─── Scroll indicator ─── */
  .hero-scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.6rem;
    color: var(--color-text-muted);
  }

  .scroll-label {
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
  }

  .scroll-line {
    width: 1px;
    height: 2.5rem;
    background: linear-gradient(to bottom, var(--color-amber), transparent);
    animation: scroll-pulse 2.2s ease-in-out infinite;
    transform-origin: top;
  }

  @keyframes scroll-pulse {
    0%, 100% { transform: scaleY(0.4); opacity: 0.4; }
    50% { transform: scaleY(1); opacity: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .scroll-line { animation: none; }
  }
</style>
