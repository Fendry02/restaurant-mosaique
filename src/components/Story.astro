<section class="story" id="histoire" aria-labelledby="story-heading">
  <div class="container">
    <div class="story-layout">
      <div class="story-visual" data-reveal="scale">
        <div class="story-photo-stack">
          <div class="story-photo story-photo--main">
            <div class="story-photo-placeholder">
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
                <circle cx="8.5" cy="8.5" r="1.5"/>
                <polyline points="21 15 16 10 5 21"/>
              </svg>
              <span>Photo du restaurant</span>
            </div>
          </div>
          <div class="story-photo story-photo--accent">
            <div class="story-photo-placeholder story-photo-placeholder--warm">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
                <circle cx="8.5" cy="8.5" r="1.5"/>
                <polyline points="21 15 16 10 5 21"/>
              </svg>
              <span>La broche</span>
            </div>
          </div>
        </div>
        <div class="story-stat">
          <span class="stat-number">3600+</span>
          <span class="stat-label">avis Google</span>
        </div>
      </div>

      <div class="story-text" data-reveal="right">
        <p class="section-subtitle" style="text-align:left">Notre histoire</p>
        <h2 id="story-heading" class="section-label" style="text-align:left">
          Le goût authentique,<br />au cœur de Lyon 3
        </h2>
        <div class="story-body">
          <p>
            Chez Mosaique, chaque jour commence pareil : la broche tourne,
            les frites sont coupées à la main, le pain est pétri et cuit
            sur place. Pas de raccourcis, pas de surgelés.
          </p>
          <p>
            Notre secret ? Des recettes transmises avec soin, des
            ingrédients sélectionnés chaque matin, et une équipe qui met
            du cœur dans chaque assiette. Du kebab classique aux
            spécialités comme l'Adana ou le Köfte, on cuisine ce qu'on
            aime manger.
          </p>
          <p>
            Depuis le 42 avenue Félix Faure, on régale le quartier — et
            au-delà. Passez nous voir, on vous accueille avec le sourire
            et un thé.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .story {
    padding-block: var(--space-section);
    background: var(--color-charcoal-mid);
  }

  .story-layout {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: var(--space-xl);
    align-items: center;
  }

  /* ─── Visual column ─── */
  .story-visual {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
  }

  .story-photo-stack {
    position: relative;
    width: 100%;
    max-width: 320px;
  }

  .story-photo--main {
    width: 100%;
    aspect-ratio: 4 / 5;
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 1px solid rgba(232, 148, 58, 0.15);
  }

  .story-photo--accent {
    position: absolute;
    bottom: -1rem;
    right: -1.5rem;
    width: 45%;
    aspect-ratio: 1;
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 3px solid var(--color-charcoal-mid);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }

  .story-photo-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: linear-gradient(135deg, var(--color-charcoal) 0%, var(--color-charcoal-light) 100%);
    color: rgba(245, 240, 232, 0.3);
    font-size: var(--text-xs);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .story-photo-placeholder--warm {
    background: linear-gradient(135deg, #3E2A1A 0%, #5C3A20 50%, #4A2E18 100%);
  }

  .story-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: var(--space-sm) var(--space-md);
    background: rgba(232, 148, 58, 0.08);
    border: 1px solid rgba(232, 148, 58, 0.15);
    border-radius: var(--radius-lg);
  }

  .stat-number {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-amber);
  }

  .stat-label {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-weight: 500;
  }

  /* ─── Text column ─── */
  .story-text {
    max-width: 36rem;
  }

  .story .section-label {
    color: var(--color-cream);
  }

  .story-body {
    margin-top: var(--space-md);
  }

  .story-body p {
    color: var(--color-text-muted);
    line-height: 1.8;
  }

  .story-body p + p {
    margin-top: var(--space-sm);
  }


  @media (max-width: 768px) {
    .story-layout {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .story-text .section-subtitle,
    .story-text .section-label {
      text-align: center;
    }

    .story-visual {
      order: -1;
    }

    .story-photo-stack {
      max-width: 240px;
    }

    .story-photo--accent {
      right: -1rem;
    }

    .story-highlights {
      align-items: center;
    }
  }
</style>
