<section class="story" id="histoire" aria-labelledby="story-heading">
  <div class="container">
    <header class="story-head" data-reveal>
      <p class="eyebrow">Chapitre II · Notre Histoire</p>
      <h2 id="story-heading" class="story-title">
        Un kebab sympa,<br />
        <em>au cœur de Lyon 3<sup>e</sup></em>
      </h2>
    </header>

    <div class="story-layout">
      <div class="story-text">
        <p class="story-lede drop-cap" data-reveal>
          Chez Mosaique, on fait du kebab comme on l'aime : viande à la broche,
          portions généreuses, et un accueil simple. Un kebab de quartier,
          sans chichis.
        </p>

        <blockquote class="pull-quote story-quote" data-reveal="left">
          « Du kebab, des grillades, des spécialités turques.
          Une adresse sympa où bien manger sans se ruiner. »
        </blockquote>

        <p class="story-body" data-reveal>
          Du kebab classique aux spécialités comme l'Adana ou le Köfte,
          on régale le quartier depuis le 42 avenue Félix Faure. Sur place,
          à emporter — passez nous voir, on vous accueille avec le sourire.
        </p>

        <div class="story-signature" data-reveal>
          <span class="sig-line"></span>
          <span class="sig-text">L'équipe Mosaique · Lyon 03</span>
        </div>
      </div>

      <aside class="story-aside" data-reveal="right">
        <!-- Giant editorial stat -->
        <div class="story-stat-block">
          <span class="stat-kicker">Sur Google</span>
          <span class="display-num story-stat-num">3.6<span class="stat-unit">k+</span></span>
          <span class="stat-descr">avis clients, note moyenne de 4,3 / 5</span>
        </div>

        <hr class="rule" />

        <ul class="story-facts">
          <li><span class="fact-num">01</span><span>Viande à la broche grillée en continu</span></li>
          <li><span class="fact-num">02</span><span>Portions généreuses, prix doux</span></li>
          <li><span class="fact-num">03</span><span>Sur place ou à emporter</span></li>
          <li><span class="fact-num">04</span><span>Viande halal, large choix de sauces</span></li>
        </ul>
      </aside>
    </div>
  </div>
</section>

<style>
  .story {
    padding-block: var(--space-section);
    background: var(--color-charcoal-mid);
    position: relative;
    overflow: hidden;
  }

  /* Watermark behind */
  .story::before {
    content: 'II';
    position: absolute;
    right: -3vw;
    bottom: -8vh;
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 700;
    font-size: clamp(20rem, 40vw, 40rem);
    line-height: 0.7;
    color: var(--color-amber);
    opacity: 0.04;
    pointer-events: none;
    user-select: none;
    z-index: 0;
  }

  .story > .container {
    position: relative;
    z-index: 1;
  }

  .story-head {
    max-width: 48rem;
    margin-bottom: var(--space-xl);
  }

  .story-title {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-cream);
    line-height: 0.95;
    letter-spacing: -0.02em;
    margin-top: var(--space-md);
  }

  .story-title em {
    font-style: italic;
    color: var(--color-amber);
  }

  .story-layout {
    display: grid;
    grid-template-columns: 1.4fr 1fr;
    gap: var(--space-xl);
    align-items: start;
  }

  /* Text column */
  .story-text {
    max-width: 38rem;
  }

  .story-lede {
    font-size: var(--text-lg);
    color: var(--color-cream);
    line-height: 1.7;
  }

  .story-lede::after {
    content: '';
    display: block;
    clear: both;
  }

  .story-quote {
    margin: var(--space-lg) 0;
    font-size: var(--text-xl);
  }

  .story-body {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    line-height: 1.8;
  }

  .story-signature {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: var(--space-lg);
  }

  .sig-line {
    width: 3rem;
    height: 1px;
    background: var(--color-amber);
  }

  .sig-text {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  /* Aside column */
  .story-aside {
    position: sticky;
    top: 6rem;
    padding: var(--space-lg) var(--space-md);
    border: 1px solid var(--color-rule-warm);
    background: rgba(26, 23, 20, 0.4);
    border-radius: 2px;
  }

  .story-stat-block {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .stat-kicker {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  .story-stat-num {
    font-size: clamp(4.5rem, 9vw, 7rem);
    display: flex;
    align-items: baseline;
    gap: 0.15rem;
  }

  .stat-unit {
    font-size: 0.5em;
    color: var(--color-amber);
    opacity: 0.7;
  }

  .stat-descr {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: 1.5;
    max-width: 20ch;
  }

  .story-facts {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .story-facts li {
    display: grid;
    grid-template-columns: 2.5rem 1fr;
    gap: 0.75rem;
    align-items: baseline;
    padding-block: 0.9rem;
    font-size: var(--text-sm);
    color: var(--color-cream);
    border-bottom: 1px dashed rgba(245, 240, 232, 0.09);
  }

  .story-facts li:last-child { border-bottom: none; }

  .fact-num {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    color: var(--color-amber);
    font-weight: 500;
  }

  @media (max-width: 900px) {
    .story-layout {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }
    .story-aside {
      position: static;
    }
  }
</style>
